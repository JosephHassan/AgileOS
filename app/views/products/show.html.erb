<p id="notice"><%= notice %></p>

<%= render 'products/detail' %>

<%= link_to 'Edit', edit_product_path(@product) %> |
<%= link_to 'Back', products_path %> | 
<%= link_to 'Add a Release to this product', new_release_path(@product)%> | 
<%= link_to 'Add an Epic to this product', new_epic_path(@product)%>

<h2 class="">Current Releases for this product</h1>

<%= render 'releases/list'%>

<h2>Current Stories for this product</h2>

<%= render 'stories/list' %>
